void main() {
  var a = 10;

  // a = a + 10;
  // a += 10;
  // a -= 10;
  // a *= 10;
  // // a /= 10;
  // a ~/= 10;
  // a %= 10;

  // print(a);

  // increment decrement
  var i = 0;
  // i++; // i = i + 1;

  // var j = i++; 0
  var j = ++i; //j = i, i++
  var k = ++i; // k= (++1)

  print(j);
  print(k);
}
