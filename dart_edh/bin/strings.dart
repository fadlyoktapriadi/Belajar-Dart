void main(List<String> args) {
  // String s = "This a \n \"String\"";
  // String s = "This a 'String'";
  // String s = 'This a "String"';

//   String s = '''' cek cek
// aaa''';

  // int number = 10;
  // double area = 40.123;

  // String s = 'My Number is ' + number.toString();
  // String s = 'My Number is ' + number.toString();

  // String s = 'The area of my home is ${area + 10} square meters. ';

  // String s = 'Hello ${String.fromCharCode(128125)}';

  // s = s.toUpperCase();
  // s = s.toLowerCase();
  // s = s.substring(0, 5); //potong string

  String s = 'My Name is Asep';

  // bool b = s.isNotEmpty;
  // bool b = s.isEmpty;
  // bool b = s.contains(Asep);
  // bool b = s.startsWith(A);
  // bool b = s.endsWith(P);

  List<String> strings = s.split(' ');

  print(strings);

  // print(s);
}
