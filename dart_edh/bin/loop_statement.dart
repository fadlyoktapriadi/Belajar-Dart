import 'dart:io';

void main(List<String> args) {
  String? input = stdin.readLineSync() ?? '';
  int number = int.tryParse(input) ?? 0;

  // String? input;
  // int number;

  // for (int counter = 0; counter < limit; counter++) {
  //   print('Hello ');
  // }

  // while (number >= 0) {
  //   String? input = stdin.readLineSync() ?? '';
  //   number = int.tryParse(input) ?? 0;
  // }
  // print('input terakhir adlaah $number');

  // do {
  // } while ();
}
